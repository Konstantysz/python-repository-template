[project]
name = "python-repository-template"
version = "0.1.0"
description = "Copier template for modern Python projects"
requires-python = ">=3.12"

[project.optional-dependencies]
dev = [
    "copier>=9.0",
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.9",
]